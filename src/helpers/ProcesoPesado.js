export const ProcesoPesado = (iteraciones) => {
  for (let i = 0; i < iteraciones; i++) {
    console.log("Vamos cargando compnonente");
  }

  return ` ${iteraciones} Interacciones en el counter`;
};
